import { DOMAIN_MODULES } from './kernel/modules'

export const foundationSnapshot = Object.freeze({
  slice: 'S0 · Foundation',
  architecture: 'Modular monolith · V40',
  status: 'Greenfield branch isolated',
  modules: DOMAIN_MODULES,
})
